# pythonw.exe stderr crash — full evidence & PR brief (2026-07-31)

Companion to SKILL.md §4 "Windows: Gateway dies at boot". Everything needed to
re-diagnose the crash or hand the upstream fix to a coding agent.

## Crash traceback (from `gateway-exit-diag.log`)

```
RuntimeError('sys.stderr is None')
  File "C:\Users\dalia\AppData\Local\hermes\hermes-agent\hermes_cli\gateway.py", line 5024, in run_gateway
    success = asyncio.run(start_gateway(replace=replace, verbosity=verbosity))
  File "C:\Users\dalia\AppData\Roaming\uv\python\cpython-3.11-windows-x86_64-none\Lib\asyncio\runners.py", line 190, in run
  File "...\asyncio\base_events.py", line 654, in run_until_complete
  File "C:\Users\dalia\AppData\Local\hermes\hermes-agent\gateway\run.py", line 24213, in start_gateway
    success = await runner.start()
  File "C:\Users\dalia\AppData\Local\hermes\hermes-agent\gateway\run.py", line 7821, in start
    faulthandler.enable()
RuntimeError: sys.stderr is None
```

Observed on EVERY auto-start attempt since 2026-07-27 (07-27, 07-28, 07-29,
07-30, 07-31 09:13) — all with `"stdin_is_tty": false,
"absorb_windows_console_controls": true` (VBS login-item launch). Manual
console launches (`hermes gateway start`, `stdin_is_tty: true`) succeed.

## Evidence file paths (Windows)

- Exit diagnostics: `~/AppData/Local/hermes/profiles/poncho/logs/gateway-exit-diag.log`
- Gateway log: `~/AppData/Local/hermes/profiles/poncho/logs/gateway.log`
  (ends at "Starting Hermes Gateway..." with no "✓ slack connected" when crashed)
- Startup launcher: `%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup\Hermes_Gateway_poncho.vbs`
  → chains to `~/AppData/Local/hermes/profiles/poncho/gateway-service/Hermes_Gateway_poncho.vbs`
- Repo (live install): `C:\Users\dalia\AppData\Local\hermes\hermes-agent`
  (git remote `github.com/NousResearch/hermes-agent`, branch `main`)

## The offending launcher line

```vbs
sh.Run "C:\Users\dalia\AppData\Roaming\uv\python\cpython-3.11-windows-x86_64-none\pythonw.exe -m hermes_cli.main --profile poncho gateway run", 0, False
```

Local hotfix: `pythonw.exe` → `python.exe` (window style `0` keeps the console
hidden; stderr exists so faulthandler doesn't crash). Note the VBS is
regenerated by `hermes gateway install`.

## Ready-made Claude Code brief (upstream fix + PR)

Paste into Claude Code with the `hermes-agent` folder as the workspace:

> **Fix a Windows startup crash in the Hermes Gateway and open a PR.**
>
> **Repo:** This workspace (`C:\Users\dalia\AppData\Local\hermes\hermes-agent`, git remote `github.com/NousResearch/hermes-agent`, branch `main`).
>
> **Bug:** After Windows reboot, the gateway never starts. The auto-start login item runs a generated VBS that launches the gateway with **`pythonw.exe`** — windowless Python, which has **no stderr (`sys.stderr is None`)**.
>
> **Crash:** `faulthandler.enable()` inside `gateway/run.py` (~line 7821, in `start()`) throws `RuntimeError: sys.stderr is None` and the gateway exits immediately. Evidence: the exit-diag log shows the identical traceback on every auto-start attempt since Jul 27.
>
> **Fix — do both:**
> 1. Guard `faulthandler.enable()` so it only runs when `sys.stderr is not None` (defensive; must not change behavior on Linux/macOS).
> 2. Fix the Windows `gateway install` VBS **generator** in `hermes_cli/` to launch `python.exe` (with a hidden window) instead of `pythonw.exe`, so stderr exists. Keep the hidden-window behavior — no console may pop up.
>
> **Note:** a local hotfix already switched the generated VBS on this machine to `python.exe` — use it as the reference for desired behavior, but the real fix belongs in the generator so future installs are correct.
>
> **Verification:** run any existing tests for this path; then simulate the boot launch (hidden window, `python.exe -m hermes_cli.main --profile poncho gateway run`) and confirm the gateway connects to Slack instead of crashing.
>
> **Deliverable:** commit on a new branch (e.g. `fix/gateway-pythonw-stderr-crash`), push, and open a PR to upstream (fork first if no push access to NousResearch/hermes-agent). Explain root cause + fix in the PR description.

## Operational context

- While the gateway is down, Slack messages are NOT received by the agent —
  but outbound alerts via `chat.postMessage` still work (send-only). A dead
  gateway silently breaks inbound Slack (e.g. Dalia's confirmation replies),
  which is how missed confirmations happen.
- `hermes gateway status` remains the fastest liveness check; the log line
  "✓ slack connected" is the definitive success marker.
