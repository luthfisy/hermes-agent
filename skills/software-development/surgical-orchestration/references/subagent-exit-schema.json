{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SubagentExitPayload",
  "description": "The JSON a Worker/Verifier/Test-Fixer emits as its final output. The orchestrator parses this and derives the canonical loop-detection hash from it.",
  "type": "object",
  "required": ["status", "debrief", "self_audit"],
  "properties": {
    "status": {
      "type": "string",
      "enum": ["COMPLETED", "FAILED"],
      "description": "COMPLETED only if the mission objective was met. Unparseable output is treated as FAILED."
    },
    "files_modified": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Paths modified, relative to the allowed scope. Sorted into the hash, so order does not matter."
    },
    "filesModified": {
      "type": "array",
      "items": { "type": "string" },
      "description": "camelCase alias accepted by parseSubagentResult alongside files_modified."
    },
    "debrief": {
      "type": "string",
      "description": "Concise changes, trade-offs, verification performed. Must be deterministic: no timestamps, no run ids — non-deterministic text defeats duplicate detection."
    },
    "self_audit": {
      "type": "string",
      "description": "Answer to: 'Is that the best I could do following best practices?'"
    },
    "selfAudit": {
      "type": "string",
      "description": "camelCase alias accepted by parseSubagentResult alongside self_audit."
    }
  }
}
