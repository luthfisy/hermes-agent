# Harness Report — <project name>

> Generated by `better-harness` (Hermes adaptation of QoderAI/better-harness).
> Evidence-bound review of the agent work loop. Scores are ceilings anchored to
> evidence states (Present ≤ 74, Wired ≤ 84, Exercised ≤ 94, Outcome ≤ 100) —
> not a code-quality score.

## Scope

- **Target:** `<repo path>` · **Window:** `<since> → <until>` · **Depth:** quick / normal
- **Acceptance boundary:** `<what "done" means for this review>`
- **Lanes available:** Session ✓/✗ · Project ✓/✗ · Agent Customize ✓/✗
  (unavailable lanes are explicit, never papered over)

## Loop Effectiveness scorecard

| Dimension | Score | Highest evidence | Summary |
| --- | --- | --- | --- |
| Task Understanding | `--` | Present/Wired/Exercised/… | <one sentence> |
| Controlled Execution | `--` | … | <one sentence> |
| Change Validation | `--` | … | <one sentence> |
| Reliable Delivery | `--` | … | <one sentence> |
| Learning Capture | `--` | … | <one sentence> |

## Findings

### 1. `<title — a concrete observed consequence>`

- **Severity:** Low / Medium / High
- **Dimension:** `<dimension>` → `<primary check>`
- **Reason:** fact — inference — owner — uncertainty (kept separate)
- **Owner:** `<smallest owner>` · **Evidence:** `<bounded refs>`
- **Expected output:** `<verifiable outcomes>`
- **Verifier:** `<the check that proves the repair landed>`

### 2. … (retain every distinct supported finding; five is a floor, not a target)

## Support track

**<Bootstrap / Operationalize / Optimize / Undetermined>** — <one-line
justification from the evidence>

Priority moves (at most three, shaped by the track; never adds findings or
rescales scores):

1. <move>
2. <move>
3. <move>

## Next steps

- Finding-bound fixes: apply at the smallest owner, run owner-owned validation,
  then an independent `verified | partial | blocked` review. Repair updates
  Repair Progress only; Loop Effectiveness waits for a comparable later Task
  Episode.
- Unavailable evidence (`Unobserved`) must be resolved before the next review
  can claim improvement.
