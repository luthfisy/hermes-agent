name: subagent-handles
version: 0.4.0
description: Durable subagent handle registry (hooks + restart reconciliation) + read-only handle introspection
author: michael-anselmi
license: MIT
kind: standalone
provides_tools:
  - subagent_handles
provides_hooks:
  - subagent_start
  - subagent_stop
