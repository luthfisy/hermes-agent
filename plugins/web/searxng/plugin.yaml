name: web-searxng
version: 1.1.0
description: "SearXNG web search — free, self-hosted, privacy-respecting metasearch engine. Configurable via web.searxng in config.yaml (method, params, headers) or SEARXNG_URL env var."
author: NousResearch
kind: backend
provides_web_providers:
  - searxng
