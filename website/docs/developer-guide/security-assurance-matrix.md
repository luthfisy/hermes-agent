---
title: "Security assurance matrix"
description: "Test-linked security guarantees, partial coverage, and known gaps"
---

# Hermes security assurance matrix

> This document is generated by `scripts/generate_security_parity.py`. Do not edit it by hand.

This matrix distinguishes tested guarantees from partial coverage and explicit gaps. A linked test is evidence for the stated behavior only; it is not proof against every bypass.

| Guarantee | Status | Evidence | Honest limit |
|---|---|---|---|
| <a id="catastrophic-command-floor"></a>Catastrophic shell commands are blocked before approval or YOLO policy. | ✅ proven | [`test_approval.py`](../../../tests/tools/test_approval.py) | This is command-shape enforcement, not an OS capability sandbox. |
| <a id="dangerous-command-approval"></a>Dangerous terminal actions require the configured human or smart-approval decision. | ✅ proven | [`test_approval.py`](../../../tests/tools/test_approval.py)<br>[`test_approval_outcome_parity.py`](../../../tests/tools/test_approval_outcome_parity.py) | Only actions classified as dangerous enter this approval path. |
| <a id="user-deny-floor"></a>User-defined command deny rules outrank approval and YOLO policy. | ✅ proven | [`test_approval_deny_rules.py`](../../../tests/tools/test_approval_deny_rules.py) | Deny rules parse command text; renamed binaries and runtime-built commands need containment. |
| <a id="file-write-protection"></a>File mutation tools enforce protected paths and configured safe roots. | ✅ proven | [`test_file_write_safety.py`](../../../tests/tools/test_file_write_safety.py) | The guarantee covers Hermes file tools, not arbitrary writes by an allowed subprocess. |
| <a id="execute-code-approval"></a>execute_code carries the calling session's approval identity and unattended policy. | ✅ proven | [`test_execute_code_approval_cluster.py`](../../../tests/tools/test_execute_code_approval_cluster.py) | A documented headless-local compatibility path remains present but not yet proven uniformly safe. |
| <a id="cross-surface-approval-parity"></a>Equivalent approval outcomes are consistent across interactive and gateway surfaces. | ◐ partial | [`test_approval_mode_parity.py`](../../../tests/tools/test_approval_mode_parity.py)<br>[`test_approval_outcome_parity.py`](../../../tests/tools/test_approval_outcome_parity.py) | Not every tool family, backend, delegated child, cron path, and messaging surface is in one matrix. |
| <a id="structured-security-reasons"></a>Security decisions expose a closed, machine-readable reason-code vocabulary. | ◐ partial | [`test_approval_reason_codes.py`](../../../tests/tools/test_approval_reason_codes.py) | Approval denials, pending decisions, and hard floors are covered; other tool families still need migration. |
| <a id="raise-only-policy-composition"></a>Independent security rules can raise a verdict but cannot weaken another rule. | ○ gap | — | Hermes has hard floors, but no general PASS/AUTH/BLOCK reducer contract for all tools. |
| <a id="cross-call-taint-correlation"></a>Sensitive values read in one call are correlated with later outbound calls. | ○ gap | — | Redaction and credential isolation do not establish cross-call information-flow tracking. |
| <a id="tool-output-secret-gate"></a>Credential-bearing tool output is withheld before it reaches the model. | ○ gap | — | Output redaction is not yet catalogued as an execution-path blocking guarantee. |
| <a id="raise-only-policy-change"></a>An agent cannot silently weaken previously accepted security policy. | ○ gap | — | Hermes protects secret paths, but policy weakening is not represented by a pinned monotonic contract. |

## Interpretation

`partial` means the behavior is present but not yet proven uniformly across every relevant path. `gap` means no Hermes-wide enforcement contract was verified during this review.

No security or compliance guarantee should be inferred beyond the exact behavior and limits listed here.
