# Synthetic sample input

> **SYNTHETIC FIXTURE — not real product or screenshot data.** This text description is invented and cannot establish actual visual measurements.

User goal: a returning user wants to export the latest monthly report on a phone.

Description: The screen title says “Reports.” A primary-looking blue button says “Create.” The export action is an unlabeled icon in the top-right. A list contains three rows named “Monthly report,” all without dates. Selecting a row opens a menu with “Download CSV” and “Delete” adjacent. No confirmation behavior is described.

Expected task: use text-described fallback, critique only the described labels/flow, and mark visual hierarchy, contrast, spacing, touch size, and hidden states as unverified.
