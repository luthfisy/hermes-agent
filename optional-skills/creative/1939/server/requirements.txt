# Required for MCP server (bundled FastMCP is in mcp.server.fastmcp)
mcp>=1.0.0,<2.0.0

# Optional — improve fuzzy matching (server degrades gracefully without these):
# metaphone
# python-Levenshtein